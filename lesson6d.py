#modules number2 : in built modules
# They can be pre-installed with the python interprator
from  math import sqrt, factorial

# import math
import datetime

# math.factorial

print(sqrt(81))
print(factorial(10))
print(datetime.datetime.now())
